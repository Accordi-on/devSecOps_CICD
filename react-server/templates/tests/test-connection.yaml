apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "react-server.fullname" . }}-test-connection"
  labels:
    {{- include "react-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "react-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
