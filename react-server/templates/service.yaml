apiVersion: v1
kind: Service
metadata:
  name: {{ include "react-server.fullname" . }}
  labels:
    {{- include "react-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "react-server.selectorLabels" . | nindent 4 }}
